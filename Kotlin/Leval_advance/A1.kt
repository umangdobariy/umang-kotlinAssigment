package Assigment.Leval_advance


//WAP to implement the function so that it returns the
//index of the largest element in the array, or null if the array is
//empty. ( fun indexOfMax(a: IntArray): Int? ) (Advanced)

fun main() {

    println("max number is :${indexOfMax(intArrayOf(1,2,3,4,5,6)).maxOrNull()}")
}
fun indexOfMax(a:IntArray): IntArray {
    return a
}
